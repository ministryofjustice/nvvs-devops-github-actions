name: Add issues to project

on:
  issues:
    types:
      - opened

jobs:
  call-workflow:
    uses: ministryofjustice/cloud-operations/.github/workflows/issue-to-project.yml@main
    with:
      username: mona
    secrets:
      envPAT: ${{ secrets.TECH_SERVICES_GITHUB_TOKEN }}
